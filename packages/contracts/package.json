{
  "name": "contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Blockchain Lost & Found System",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.ts",
    "deploy:basesepolia": "npx hardhat run scripts/deploy.ts --network baseSepolia"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.4.2",
    "hardhat": "^2.22.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
